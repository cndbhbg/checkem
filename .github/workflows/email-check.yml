name: Check Gmail

on:
  push:  # Kích hoạt khi có push lên repository
  schedule:
    - cron: '* * * * *'  # Chạy mỗi giờ

jobs:
  check-email:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt  # Cài đặt các thư viện từ requirements.txt

    - name: Run the script
      run: python chek.py
